/** Dim `◌ exploring` label shown in place of a proficiency bar for skills being learned. */
export function ExploringTag() {
  return (
    <span className="font-mono text-[10px] ml-2 flex-shrink-0 text-muted">
      ◌ exploring
    </span>
  );
}
