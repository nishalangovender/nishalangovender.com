import { BeatDashboard } from "@/components/sections/hero-loop/beats/BeatDashboard";

import { BeatFrame } from "../BeatFrame";

export default function Beat5Page() {
  return <BeatFrame title="Beat 5 — Zoom Out to Dashboard" window="13.5–16.5s" Beat={BeatDashboard} />;
}
