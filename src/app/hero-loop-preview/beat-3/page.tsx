import { BeatBoot } from "@/components/sections/hero-loop/beats/BeatBoot";

import { BeatFrame } from "../BeatFrame";

export default function Beat3Page() {
  return <BeatFrame title="Beat 3 — Boot Sequence" window="6.5–9.0s" Beat={BeatBoot} />;
}
