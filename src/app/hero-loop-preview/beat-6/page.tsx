import { BeatReturn } from "@/components/sections/hero-loop/beats/BeatReturn";

import { BeatFrame } from "../BeatFrame";

export default function Beat6Page() {
  return <BeatFrame title="Beat 6 — Return to Desk" window="16.5–20.0s" Beat={BeatReturn} />;
}
