import { BeatSketch } from "@/components/sections/hero-loop/beats/BeatSketch";

import { BeatFrame } from "../BeatFrame";

export default function Beat1Page() {
  return <BeatFrame title="Beat 1 — Sketch Establishes" window="0.0–3.5s" Beat={BeatSketch} />;
}
